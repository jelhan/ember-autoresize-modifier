{
  "private": true,
  "repository": {
    "type": "git",
    "url": "git@github.com:jelhan/ember-autoresize-modifier.git"
  },
  "workspaces": [
    "addon",
    "test-app"
  ],
  "scripts": {
    "build": "pnpm --filter ember-autoresize-modifier build",
    "prepare": "pnpm build"
  },
  "devDependencies": {
    "release-plan": "0.18.0"
  },
  "pnpm": {
    "overrides": {
      "testem": "3.20.0"
    }
  }
}
